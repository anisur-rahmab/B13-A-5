1. var is old and its value cannot be changed and reassigned. So many errors have to be faced let is update can be used to change any value and const is best and its value cannot be changed and reassigned. 
2. The spread operator spreads the elements of an array.
3. map(), filter(), and forEach() — these are three JavaScript array methods. They loop over an array, but they serve different purposes.
4. Arrow function is a shortcut version of a function that makes the function more readable and the code looks cleaner. This is ES6 version.
5. Template Literals are a feature of JavaScript that allows you to easily write strings and place them inside variables or expressions.